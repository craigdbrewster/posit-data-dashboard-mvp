[project]
name = "posit-zenith-shiny"
version = "1.0.0"
description = "Posit Platform Analytics Dashboard"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"
